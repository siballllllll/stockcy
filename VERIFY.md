# 검증 원장 (VERIFY.md)

"나중에 확인하자"고 미뤄둔 것들의 단일 목록. 날짜를 사람이 기억하지 않아도 되게 하는 게 목적이다.

**현황 확인:**

```
venv/Scripts/python verify_check.py
```

이 스크립트가 실제 DB·설정을 조회해 항목별로 `[DUE] / [READY] / [WAIT] / [BLOCKED] / [DONE]`을
찍어준다. 세션을 새로 시작했거나 오랜만에 손을 대면 이것부터 돌릴 것.

> **Claude에게:** 이 저장소에서 작업을 시작할 때, 그리고 사용자가 "뭐 확인할 거 없나 / 그거 어떻게
> 됐지"류로 물으면 위 스크립트를 먼저 돌리고 결과를 근거로 답할 것. 아래 기준을 기억에 의존해
> 판정하지 말 것 — 수치는 스크립트가 준다. 항목이 해결되면 이 파일에서 지우고,
> 새로 미루는 게 생기면 같은 형식으로 추가할 것.

마지막 갱신: 2026-09-11 (V11 판정 완료 → 비용 감시기로 전환. V4·V6·V9 내림. V5 해결)

---

## V1. AI 종목분석 신뢰도 — 기한 2026-09-25

**왜 미뤘나.** v3.138.0/1에서 종목 리포트에 정량 근거 팩·수급·실측 변동폭 앵커를 넣고 프롬프트의
하락 편향을 걷어냈다. 개선이 실제로 먹혔는지는 예측이 시간을 통과해봐야 알 수 있다.

**어떻게 판정하나.** `analysis_history`에 쌓인 단기 전망(`short_term_view_pct`)과 실제 주가를 대조.

**통과 기준.** 방향 판정 20건+ AND 단기 전망 **방향** 적중률 55%+.
(현황판은 예측폭 평균오차와 변동폭 절단 발생률도 함께 찍어준다 — 보조 지표로 볼 것.)

**선행 조건 — 해결됨 (v3.140.0).** `analysis_history`에 `base_price`/`d1·d3·d7_return`/
`outcome_checked_at`을 추가하고, `track_analysis_history_outcomes()`를 일일 작업(api/main.py 07시
이후 1회)에 연결했다. 적중률은 `analysis_hit_rate()`가 산출하며 현황판이 그대로 찍어준다.
이제 표본만 쌓이면 판정된다 — **종목분석을 실행할수록 표본이 는다(분석 1회 = 이력 1건).**

---

## V2. 자체 ML 판단 반영 복귀 — 기한 2026-09-24

**왜 미뤘나.** 09-03에 실전 AUC가 d7 0.524 / d3 0.447로 나와 복귀 기준에 미달, 판단 반영 4곳을
차단(`ML_DECISIONS=0`)했다. 원인은 학습셋의 67%를 차지하던 `scenario` 소스가 예측력 0이었던 것.
이를 제외하고 재학습해 CV AUC가 0.606 → 0.705로 올랐다(v3.136.1). 이 개선이 실전에서도
재현되는지는 재학습 이후 새로 쌓인 표본으로만 확인할 수 있다.

**어떻게 판정하나.** `ml_training_samples`에서 `decided_at >= 2026-09-03`인 `pred_d7` + `label` 쌍만
골라 AUC를 다시 계산.

**통과 기준.** 신규 구간 실전 AUC **0.60+** → 판단 반영 4곳 복귀.

**⚠️ 함정.** CV 점수(0.705)로 판정하지 말 것. 그건 학습셋 위의 점수지 실전 성능이 아니다.
09-03에 이미 "CV는 좋은데 실전은 0.524"를 겪었다. 반드시 재학습 **이후** 구간만 볼 것.

---

## V3. 섀도우 리그 승자 이식 — 1차 이식 완료, **n=50에서 재판정**

**1차 판정 (2026-09-04, 실현 172건 / 2개월).** 평균 순위로 보면 A가 1등이지만 그건 +65.5%
한 건이 만든 값이었다(중앙값 +1.28%). 랜덤 대조군(E) 대비 **승률이 통계적으로 유의한 것은 C 하나뿐**.

| 전략 | n | 평균 | 중앙값 | 승률 | vs 대조군 |
|---|---|---|---|---|---|
| **C 이슈×구간** | 41 | +2.52% | +0.73% | **61.0%** | z=+2.57, **p=0.010** |
| A 눌림목 | 40 | +3.64% | +1.28% | 52.5% | p=0.070 (미달) |
| D 수급추종 | 15 | +0.21% | -4.08% | 46.7% | p=0.331 |
| E 랜덤 | 40 | -2.82% | -4.45% | 32.5% | (기준) |
| F 모멘텀추격 | 32 | -6.57% | -8.41% | 34.4% | p=0.867 |

**C는 국내 전용 현상이다.** 국내 n=24 승률 **70.8%** (z=+3.04, **p=0.0024**) / 미국 n=17 승률
47.1% (p=0.774, 신호 없음). 같은 규칙인데 시장이 갈랐다.

**F에 대한 정정.** "랜덤보다 나쁘니 모멘텀 추격을 배제할 근거"는 **틀렸다**. 승률 34.4% vs 32.5%,
p=0.867로 랜덤과 구분되지 않는다. 평균은 낮지만 표본 32건으로는 그렇게 말할 수 없다.

**이식한 것 (v3.141.0).**
- 종목분석 정량 근거 팩에 `⭐ 이슈×지지구간 조건 충족` 한 줄 + 실측 근거 노출 (국내만)
- 에이전트 매수 게이트에 **OR 경로** 추가 — 기존 눌림목 게이트(A 조건)는 C보다 좁아서 승률이
  더 높은 형태를 통째로 막고 있었다. 조인 게 아니라 열었다. `AGENT_ISSUE_ZONE_GATE=0`으로 끔
- 조건식은 `ai_engine.issue_zone_signal()`에 두고 `shadow_league._wants_buy`의 SHADOW_C와
  **동일하게 유지할 것** — 어긋나면 실측 근거와 실제 동작이 따로 논다

**재판정 기준.** C 국내 표본 **n=50** 도달 시. 승률이 60%+로 유지되면 하드 게이트 승격 검토,
45% 아래로 떨어지면 OR 경로 회수. A는 그때 p<0.05로 올라오면 재검토.

**⚠️ 한계.** 표본이 2개월(7~8월)뿐이고, 7월은 전 전략 마이너스·8월에만 A·C가 플러스였다.
대조군 E가 두 달 다 마이너스라 시장 방향 효과는 아니지만, 국면 하나만 본 셈이다.
그리고 `linked`(재료 보유)는 국내 996종목이 해당돼 단독으로는 약한 필터다 — C의 우위는
조합에서 나온 것으로 봐야 한다.

---

## V5. 토스 Open API — **고정 IP로 해결됨 (2026-09-11). 남은 건 UI 하나**

**해결.** VPN 전용(고정) IP를 받아 토스 개발자센터 허용목록에 등록하니 통과했다.
- 등록한 IP: `89.187.161.5` (Surfshark 전용 IP, **일본** 지역)
- 확인: 토큰 발급 200, 현재가·호가창·장운영 달력 3종 모두 정상(`is_open: True`)
- **해외 IP라 국가 차단을 걱정했으나 실제로는 막히지 않았다.** 토스는 IP 허용목록만
  보고 국가는 보지 않는 것으로 보인다(표본 1건이므로 단정하진 말 것).

**이전 기록.** 토스는 IP 허용목록 방식인데 VPN 공용 출구 IP는 매번 바뀌어
`403 access_denied: IP address not allowed`가 났다. 그때 "VPN을 끄라"고 적어둔 처방은
전용 IP를 쓰면 해당되지 않는다 — **전용 IP는 등록해도 되는 예외다.**

**⚠️ 전용 IP가 바뀌거나 해지되면 다시 403이 난다.** 그때는 아래를 돌릴 것:

```
venv/Scripts/python scratch/toss_check.py
```

현재 출구 IP와 등록해야 할 값을 찍어주고, 실패 시 '등록 안 된 IP'인지 '국가 차단'인지
'키 문제'인지를 갈라준다.

**⚠️ 진단할 때 빠지기 쉬운 함정 (2026-09-11 실제로 빠짐).** `toss_api`는 `os.getenv`로 키를
읽으므로 **`.env`를 먼저 로드하지 않으면 요청을 보내지도 않고 `None`을 돌려준다** — 접속이
멀쩡한데 "토큰 발급 실패"로 보인다. 점검 스크립트는 이제 `.env`를 먼저 읽고, 키가 비어 있으면
"IP 문제가 아니다"라고 명시한다. 그리고 `get_token`이 `except Exception: return None`으로
사유를 통째로 삼키던 것도 고쳤다(HTTP 상태·응답 앞부분을 warning으로 남긴다).

**남은 실제 과제 — 화면이 조회 실패와 휴장을 구분하지 못한다 (우선순위 낮음, 사용자 보류).**
`cal={}`이면 `is_open`이 falsy → 무조건 "○ 오늘 휴장"으로 표시된다. 지금은 IP가 풀려서 안
드러나지만, 토스가 어떤 이유로든 응답을 못 주면 개장일에도 거짓 표시가 난다.
`OrderbookPanel.tsx`에서 조회 실패 시 "장운영 정보 없음"으로 나누면 된다.

---

## V7. ML 학습에 종목분석 소스를 편입할지 — 표본 40건에서 판정

**왜 있나.** v3.141.0부터 종목분석 결과가 `ml_training_samples`에 `source="stock_analysis"`로
적재된다(비추천 등급 제외). 다만 **처음부터 `ml_model.EXCLUDED_SOURCES`에 넣어 학습에서는
제외된 상태**로 시작했다.

**왜 바로 안 넣었나.** `scenario` 소스가 예측력 0인 채로 학습셋의 67%를 잡아먹어 실전 AUC를
0.524까지 끌어내린 전례가 있다(v3.136.1). 그때의 진짜 문제는 scenario를 넣은 것 자체가 아니라
**쓸모없다는 걸 학습셋을 다 오염시킨 뒤에야 알았다**는 것이다. 같은 실수를 반복하지 않는다.

**어떻게 판정하나.** `stock_analysis` 소스만으로 d7 AUC를 계산.

**통과 기준.** 이 소스 단독 AUC **0.55+** → `EXCLUDED_SOURCES`에서 제거해 학습 편입.
미달이면 제외 상태 유지(적재는 계속 — 나중에 다시 재볼 수 있게).

**⚠️ 유의점.** 종목분석 표본은 사용자가 검색한 임의 종목이라 스크리너 픽과 선택 편향이 다르다.
편향이 적어 일반화에 도움이 될 수도 있고, 스크리너 채점용 모델엔 노이즈일 수도 있다.
반드시 따로 재보고 결정할 것.

---

## V8. ML 학습에 스캔 후보 전체(scan)를 편입할지 — 표본 200건에서 판정

**왜 있나.** v3.142.0부터 에이전트가 스캔한 모든 종목이 `source="scan"`으로 적재된다
(매수/보류/매도 무관, 하루 30~40건). 다른 신규 소스와 마찬가지로 **`EXCLUDED_SOURCES`에
넣어 학습에서는 제외된 상태**로 시작했다.

**무엇을 고치려는 건가.** 지금 학습셋은 `pattern`(스크리너가 고득점 준 눌림목) 248건이 전부다.
모델이 **스크리너가 떨어뜨린 종목이 어떻게 됐는지를 한 번도 본 적이 없다.** 대조군 없는
restricted range라 "좋은 것과 나쁜 것을 구분하는" 학습이 구조적으로 안 된다 — 실전 AUC가
0.524에서 안 오르는 유력한 원인이다. 스캔 후보 전체를 넣으면 같은 유니버스 안에서
'안 산 케이스'가 표본에 들어온다.

**어떻게 판정하나.** `scan` 소스만으로 d7 AUC 계산. 그리고 `pattern + scan` 합본 AUC를
`pattern` 단독과 비교.

**통과 기준.** 합본 AUC가 pattern 단독보다 **+0.03 이상 개선** → 편입.
개선이 없으면 제외 유지.

**⚠️ 유의점 3가지.**
1. 유니버스가 즐겨찾기+보유+거래대금/등락률 상위라 **'시장 전체'가 아니다.** 편향이 없는 게
   아니라 지금보다 덜한 것 — 해석할 때 감안할 것.
2. 볼륨이 크다(월 ~1,000건). 편입하면 pattern 248건이 묻힌다 — 소스별 가중이나 샘플링을
   같이 검토할 것.
3. 같은 종목이 매일 스캔되면 겹치는 창의 표본이 생겨 자기상관이 있다. AUC를 액면 그대로
   믿지 말 것.

**기대치.** 현실적 최선은 AUC 0.55~0.60대다. 0.70은 기대하지 말 것.
**개선이 없어도 정보는 남는다** — 편향을 줄이고 표본을 4배로 늘려도 안 오르면, 그건
"데이터가 부족해서"가 아니라 "이 피처로는 7일 수익률을 예측할 수 없다"는 결론이고,
그때는 V2(ML 복귀)를 계속 붙잡을지 접을지를 근거를 갖고 정할 수 있다.

---

## V12. SHADOW_C '이슈 연관' 기준 변경 — 2026-09-09 전후를 갈라 볼 것

**무엇이 바뀌었나.** 리서치 워처가 `scenario_stocks`를 채우는 방식을 조였다(v3.168.0).
이 테이블은 SHADOW_C의 '이슈 연관(linked)' 판정 재료이므로, **판정 기준 자체가 바뀐 것이다.**

**왜.** 실측(2026-09-09): 시나리오 종목의 99%가 리서치 워처발이었고(2,672건 / 파이프라인 11건),
지수·업종 코멘터리에까지 대형주를 붙이고 있었다.
- 14일간 **삼성전자가 154개, 현대차 125개, POSCO홀딩스 97개**의 서로 다른 시나리오에 등장
- 원인 ①: 프롬프트의 출력 예시가 `{"name":"삼성전자","ticker":"005930"}`이라 모델이 그대로 베꼈다
- 원인 ②: 시나리오 파이프라인에 있는 종목 선정 규칙(연결 경로 필수·대형주 반복 금지)이 이쪽엔 전무했다
- 결과: 국내 **1,073종목**이 '재료 있음'으로 잡혀 조건의 변별력이 사라졌다

**조치 (v3.168.0).**
1. 프롬프트 예시에서 실제 종목명 제거 → 플레이스홀더
2. 지수·업종 전반 이슈는 stocks를 빈 배열로 두게 지시 + 코드에서도 등록 제외
3. 대형주 12종목은 **글에 회사 이름이 직접 등장할 때만** 등록
4. 한 이슈당 최대 3종목

**⚠️ 측정에 미치는 영향.** SHADOW_C의 기존 실적(승률 62.8%, n=43)은 **느슨한 기준**에서 나온
숫자다. 기준을 조인 뒤의 표본과 성격이 다르므로 **합쳐서 평균 내지 말 것.**
- 2026-09-09 이전 진입분 = 구 기준
- 그 이후 진입분 = 신 기준
두 구간의 승률을 따로 내고, 신 기준 표본이 20건을 넘으면 그때 비교한다.
기준이 조여졌으니 **매수 빈도는 줄고 건당 적중률은 올라야** 정상이다. 빈도만 줄고 적중률이
그대로면 이 변경은 실패다(변별력이 아니라 그냥 표본을 깎은 것).

**확인 방법.** `run_research_watch()` 반환값에 `skipped_index`·`skipped_mega`가 추가됐다.
얼마나 걸러지는지 로그에서 볼 수 있다.

---

## V10. 촉매 모멘텀(섀도우 G·H) — 실현 30건에서 판정

**가설.** 사용자의 실제 승리 패턴은 눌림목이 아니라 **"재료 있는 종목이 이미 움직이기 시작했을 때
올라타서 1~3일 안에 빠지는 것"**이다.

**어디서 나왔나 (2026-09-04, 리딩방 실거래 25건 사후 분석).**
- 매수 전일 차트 상태: MA20 이격 중앙값 **+10.1%**, 5일 모멘텀 **+11.6%**, 볼린저 %b **75.9**,
  MA60 위 76% → 눌림목이 아니라 **돌파·모멘텀 진입**이었다.
  (시스템은 정반대로 `사용자=눌림목 트레이더` 전제 위에 세워져 있고, 급등추격 하드필터가
   5일 +10%↑를 막는다 — 리딩방 25건 중 15건가량이 그 필터에 걸린다.)
- 기계적 보유 시 수익률: d1 -0.08%(52%) → **d3 +7.59%(64%)** → d7 +1.60%(60%).
  모멘텀 edge는 d7까지 가면 반감된다. 실제 사용자 청산은 +5.97%/96%로 모든 기계적 보유를 이겼다.
- **대조군 검정**: 같은 날짜·같은 모멘텀 대역인데 사지 않은 종목 16개와 비교 →
  강한 촉매 보유 **58%(리딩방) vs 25%(대조군)**, z=+2.08, **p=0.038 유의**.
  성과는 +6.02%/96% vs -1.38%/38%(d3). 모멘텀은 양쪽 다 있었으므로 갈린 축은 촉매 강도.
  대조군 내부에서 '이슈 있음'은 도움이 안 됐다(-4.12%) — **'강'만이 갈랐다.**

**⚠️ 위 실측의 한계.** 사후 검색이라 촉매 강도 판정에 결과가 섞였을 수 있다(오른 종목은 기사도
많다). n도 24 vs 16으로 작고 4~6월 한 국면뿐이다. **그래서 사후 분석만으로 결론 내지 말 것.**

**전향 검증 설계 (v3.145.0).** 그 오염을 없애는 방법은 **매수 전에** 같은 질문을 던지는 것이다.
- `ai_engine.catalyst_strength()` — 검색으로 촉매 강도를 판정(종목·날짜 캐시, 하루 12건 상한)
- `SHADOW_G` 촉매모멘텀 + **4일(≈3거래일)** 청산
- `SHADOW_H` 동일 진입 + 기존 10일 청산
- 진입 조건은 완전히 동일: 5일 모멘텀 +3~60% AND 촉매 강도 "강"

**어떻게 판정하나.** 세 갈래로 분해된다.
| 비교 | 무엇을 재나 |
|---|---|
| G vs E(랜덤) | 이 전략이 작동하는가 |
| **G vs H** | **청산 속도가 실제로 중요한가** (d3 vs d7 가설의 직접 검정) |
| H vs A·C | 같은 청산 조건에서 진입 방식의 우열 |

**통과 기준.** 실현 30건 이상에서 랜덤 대조군(E) 대비 승률이 유의하게 높을 것(p<0.05).
G가 H보다 유의하게 좋으면 "빠른 청산"이 검증된 것이고, 그때 메인 에이전트의 급등추격
하드필터를 **보유기간별로 분리**하는 근거가 된다(지금은 d7 전제의 필터를 단타에도 적용 중).

**⚠️ G/H 쌍을 깨지 말 것.** H를 지우면 G의 성적이 진입 덕인지 청산 덕인지 알 수 없게 된다.

**분모 보존 (v3.149.0).** 이 검증의 핵심 비교는 "같은 모멘텀 대역에서 촉매가 갈랐는가"라,
**판정했지만 사지 않은 종목**이 대조군이자 분모다. 그런데 판정 결과가 `ai_cache`의
`cat_str_*`(TTL 12시간)에만 남고 `load_ai_cache`가 만료분을 읽는 즉시 지워, 하루 만에
증발하고 있었다(9/4 매수분 CEG·NVDA·INTC의 판정 기록이 이미 소실). 이제
`catalyst_verdicts` 테이블에 영구 기록한다 — 판정일·티커·시장·5일 모멘텀·강도·매수 여부.
`bought_by`가 빈 행이 대조군이다. `db.load_catalyst_verdicts(days)`로 퍼널을 본다.

**촉매 조회 상한 (2026-09-11, v3.172.0).** `CATALYST_DAILY_LOOKUPS`가 "하루 12건"이라고 적혀
있었지만 실제로는 사이클마다 예산이 리셋돼 **사실상 상한이 없었다**(실측 하루 26~36건).
날짜 단위 예산으로 고치면서 **상한값은 12가 아니라 40으로 뒀다** — 12로 되돌리면 조회가
3분의 1로 줄어 이 검증의 표본 축적이 더 느려지기 때문이다. 비용은 하루 30원(전체의 6.9%)이라
감당된다. **V10 판정이 끝나면 12로 내릴 것.** 지금까지 쌓인 `catalyst_verdicts`는 느슨한
상한 아래 모인 것이라 분모가 의도보다 넓다 — 퍼널 비율을 볼 때 감안할 것.

**표본이 안 쌓이면 무엇부터 볼지 (2026-09-07 정정).** G/H 매수가 9/4 이후 끊긴 것을
처음엔 "스캔 루프가 죽었다"고 판단했으나 **오진이었다.** 9/5는 토요일, 9/6은 일요일로
애초에 휴장이었다(9/4가 금요일). 섀도우 전원이 0건인 게 정상이다.

오진의 원인은 두 가지였고 둘 다 고쳤다:
- 요일을 확인하지 않고 9/5를 금요일로 단정했다. **날짜 기반 판단 전에 요일부터 확인할 것.**
- 에이전트 로거가 uvicorn 아래에서 INFO를 못 찍어(v3.149.0에서 수정) "휴장 중 건너뜀"이
  로그에 안 남았고, 그래서 조용히 정상 동작하던 루프가 죽은 것처럼 보였다.

**그래도 유효한 판정 순서:** 표본이 안 쌓이면 ① 그날이 거래일인지 ② 랜덤 대조군(E)도
0건인지(0이면 루프·유니버스 문제, E만 매수했으면 G/H 필터 문제) ③ 로그에 "1주기 스캔
완료"가 찍혔는지 순으로 본다.

**⚠️ `--reload`는 동작하지 않는다 — 코드를 고치면 반드시 수동 재시작할 것 (2026-09-07 실측).**
uvicorn을 `--reload`로 띄우지만 파일 변경에 반응하지 않는다. `shadow_league.py`를 touch하고
20초를 기다려도 워커 PID가 그대로였다(60408 → 60408). 실제로 13:59에 뜬 프로세스가 21시까지
옛 코드로 돌았고, 그 사이 15:05에 커밋한 락 수정이 적용되지 않아 촉매 기록이 계속 유실됐다.
watchfiles 1.2.0은 설치돼 있으므로 원인은 경로의 한글(`스톡시`)로 추정된다 — 같은 계열의
문제로 curl_cffi가 CA 인증서 경로의 한글을 못 읽는 사례가 있다.

(앞서 이 자리에 "장중 편집이 --reload로 스캔을 죽인다"고 적었던 것은 오진이다. 근거로 삼은
`Loop Started` 2회는 리로드가 아니라 중복 기동된 프로세스 2개가 각자 띄운 것이었다.)

**재시작 방법.** `스톡시_재시작.bat`은 `.vbs`를 부르는데, 이때 프론트·프록시·터널까지 함께
뜨면서 **중복 기동될 수 있다**(2026-09-07 실측: uvicorn 2개·cloudflared 2개가 동시에 떠서
에이전트 루프가 2벌 돌았다 — Gemini 비용 2배). 백엔드 코드만 바꿨다면 uvicorn만 교체하는 것이
안전하다:
1. `python.exe` 중 `-m uvicorn api.main:app`인 프로세스를 모두 종료(자식 spawn 프로세스까지)
2. 포트 8000이 실제로 해제됐는지 `netstat`으로 확인 — 종료 요청 후에도 남아 있는 경우가 있다
3. 프로젝트 루트에서 `.\venv\Scripts\python.exe -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload` 재기동
4. 기동 후 포트 8000 점유가 **하나뿐인지**와 `Loop Started`가 **1회만** 찍혔는지 확인

**🚨 시세 오류 오염 구간: 2026-09-02 ~ 09-09 (이 구간은 측정에서 제외).**

국내 현재가가 **전일 종가로 반환되는 버그**가 이 기간 내내 매수·매도 양쪽에 영향을 줬다
(v3.159.0/v3.161.0에서 수정). `get_kr_stock_price`가 KIS 타임아웃 시 곧바로 yfinance로
폴백했는데, yfinance 국내 일봉은 장중에 전일 종가를 최신값으로 준다.

전수 검증(섀도우 거래 371건을 FDR 일봉 [저가,고가]와 대조):
- **약 20건이 그날 나올 수 없는 값**으로 체결. 손익이 -23%대에 몰리는 것이 서명이다.
- 매도만이 아니라 **매수도** 오염됐다(예: 048770을 9/7에 9/4 종가 3,965원으로 매수).
- 별개로 액면병합 종목 8건(002780·054940)은 FDR의 소급 조정 탓이라 오류가 아니다.

**조치.**
- 상한가를 -23% 급락으로 오인해 손절된 6건(224060·069540)은 매도를 취소하고 보유를
  복원했다. 그중 가짜 손절 직후의 재매수 1건은 왕복 전체를 취소했다.
- 보유 중이던 11건은 매수가를 그날 종가로 교정했다. 가격만 올리면 쓰지 않은 현금을
  쓴 것이 되므로, 예산 기반 사이징에 맞춰 **수량을 줄이고** 잔돈만 현금으로 돌렸다.
- **이미 실현된 거래는 되돌리지 않는다.** 틀린 가격이 매매 판단 자체를 만들었기 때문에
  (모멘텀 계산·손절 발동·재매수) 가격만 고치면 일어난 적 없는 원장이 된다.

**⚠️ 그래서 V10의 실현 30건 카운트는 2026-09-09부터 다시 센다.** 이 구간의 G/H 실현분은
분모에 넣지 말 것. 09-02 이전 두 달치(SHADOW_C 43건 등)는 영향이 없다.

**재발 감지.** 손익이 -23%대에 몰리거나 청산가가 전일 종가와 일치하면 이 버그를 의심할 것.
`_source` 필드로 어느 소스가 답했는지 확인할 수 있다(kis/toss/fdr/yfinance).

**통과 시 켤 것 (v3.146.0에 미리 만들어 둠, 기본 OFF).**
`.env` 에 `AGENT_HORIZON_SPLIT=1` → 메인 에이전트에 `[단타]` 지평이 열린다.
- 진입: 5일 모멘텀 +3~60% AND 촉매 강도 '강' (섀도우 G/H와 동일 조건, 캐시 공유)
- 그 경우에만 **급등추격 하드필터·눌림목 게이트를 면제**한다. 두 규칙 다 d7 전제이기 때문
- 대신 시간손절을 14일 → **4일(≈3거래일)**로 죈다. 손절·익절 가드(-5%/+8%)는 스윙과 동일 유지
- 하루 촉매 검색 8건 상한

**⚠️ V10이 통과하기 전에는 켜지 말 것.** 지금 근거는 사후 검색 복원이라 촉매 강도 판정에
결과가 섞였을 가능성이 남아 있다(오른 종목은 기사도 많다). G/H가 전향적으로 그 오염 없이
대조군을 이겨야 이 스위치를 켤 근거가 된다. 켤 때는 켠 날짜를 여기 적어둘 것.

---

## 완료되어 내려간 항목

- **V4. 수급 스냅샷 적재** (2026-09-04 확인) — 6/23 중단 원인(15분 창 의존)을 v3.107.1에서 고쳤고
  실적재를 확인했다. 현황판이 계속 감시하므로 최신 적재가 5일 이상 밀리면 다시 알림이 뜬다.
- **V6. AI 추천 적중률 파이프라인** (v3.140.0) — 이력 테이블이 둘로 갈려 있던 것(`ai_recommendations`는
  추적 기계가 있는데 0행, `analysis_history`는 데이터가 들어오는데 결과 컬럼이 없음)을,
  데이터가 실제로 쌓이는 `analysis_history` 쪽에 추적을 붙여 해소. 적중률 판정은 V1이 이어받는다.
  구형 테이블과 `track_ai_recommendation_outcomes`는 제거하지 않고 유휴로 남겼다.
  ⚠️ 남은 간극: 구형 `log_ai_recommendation`은 ML 학습샘플도 함께 적재했으나 신 경로엔 그게 없다
  — 종목분석은 자체 ML 학습에 기여하지 않는다(V2와 별개 사안).
- **V9. 시나리오 확률 라운딩** (2026-09-11 판정) — 09-05 이후 50개 중 5의 배수 6개 **12.0%**
  (수정 전 09-04은 100%). 무작위 기대치(20% 근처)보다도 낮아 통과 기준 30%를 충분히 만족한다.
  → v3.143.0에서 실사용 함수(`analyze_custom_issue`·`_deep_dive_issue`) 양쪽에 라운딩 억제
  두 문장을 이식한 것이 원인 처방으로 맞았다고 판단. 모델 드리프트 가설은 기각.
- **V11. AI 비용 재측정** (2026-09-11 판정) — 아래 별도 항목 참조.

---

## V11 판정 기록 — AI 비용 (2026-09-11)

**결론: 월 9,731원. 1만원 문턱 바로 아래. 조치 불필요.**

기준대로 09-07(월)~09-11(금) 영업일 5일, 호출 50건 이상인 날만, 수동 실행분 제외.

| | 일평균 | 월 환산(영업일 22일) |
|---|---|---|
| OpenAI | 42원 | 927원 |
| Gemini | 400원 | 8,804원 |
| **합계** | **442원** | **9,731원** |

09-04 추정치(OpenAI 월 3,300원 / Gemini 월 1,400원 / 합계 4,700원)는 **양쪽 다 틀렸다.**
총액은 2배로 과소평가했고, 프로바이더 비중은 아예 뒤집혀 있었다(OpenAI 주도 → 실제는 Gemini 91%).
작업일 이틀이 섞인 4일 표본이 그만큼 못 믿을 것이었다.

**비용 상위 출처 (5일 합계 2,135원 기준)**

| 프로바이더 | 출처 | 호출 | 일평균 | 비중 |
|---|---|---:|---:|---:|
| gemini | `analyze_custom_issue` | 28 | 97원 | 22.8% |
| gemini | `run_research_watch` | 145 | 94원 | 22.0% |
| gemini | `_deep_dive_issue` | 22 | 92원 | 21.6% |
| openai | `analyze_autonomous_trading` | 465 | 39원 | 9.2% |
| gemini | `catalyst_strength` | 125 | 30원 | 6.9% |
| | 나머지 12개 출처 합 | 58 | 74원 | 17.5% |

**🚨 원장에 적어뒀던 처방이 틀렸다.** "줄일 곳은 자율매매 스캔 주기(`INTERVAL_SECONDS`)나
스캔 종목 수"라고 써뒀지만, 자율매매 스캔은 **호출의 55%(465회)를 차지하면서 비용은 9.2%뿐**이다
(호출당 0.4원 — gpt-4o-mini에 짧은 프롬프트라 싸다). 주기를 절반으로 줄여도 월 460원이 준다.
실제 본체는 **시나리오 2종(44.4%) + 리서치 워처(22.0%) = 66%의 Gemini 장문 호출**이다.
→ **다음에 1만원을 넘으면 손댈 곳은 시나리오 딥다이브 이슈 수(`_SCENARIO_DEEP_MAX=4`)와
리서치 워처 주기다.** 호출 수가 아니라 출력 토큰이 비용을 만든다는 걸 잊지 말 것.

**촉매 판정(`catalyst_strength`)은 예상대로 작다** — 09-04 하루 2.6원(1.7%) → 지금 30원(6.9%).
섀도우 G/H가 본격 가동되면서 늘었지만 여전히 6분의 1 수준. 다만 아래 결함이 같이 잡혔다.

**발견한 결함 — 하루 상한이 실제로는 사이클당 상한이었다 (v3.172.0에서 수정).**
`CATALYST_DAILY_LOOKUPS = 12`를 `run_shadow_cycle`이 **사이클마다 새 리스트로 초기화**하고 있었다.
장중 사이클이 10회 넘게 도니 상한이 사실상 없었다 — 실측 09-07~09-11 하루 **9·36·34·26·20건**
(의도는 12건). 예산을 모듈 전역(`_catalyst_budget_today()`, 날짜 바뀌면 리셋)으로 옮겨 고쳤다.
**상한값은 12가 아니라 40으로 뒀다.** 하루 30원이면 값이 싸고, V10이 실현 9건으로 표본 기근인데
조회를 3분의 1로 줄이면 판정이 더 밀리기 때문이다. **V10이 끝나면 12로 내릴 것.**

**앞으로.** 현황판의 V11은 판정 항목에서 **상시 감시기**로 바뀌었다 — 월 환산이 1만원을 넘을
때만 `[DUE]`로 뜨고, 평소엔 `[DONE]`으로 금액만 찍는다. 출처별 상위 5개도 함께 표시한다.
