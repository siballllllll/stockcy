# 검증 원장 (VERIFY.md)

"나중에 확인하자"고 미뤄둔 것들의 단일 목록. 날짜를 사람이 기억하지 않아도 되게 하는 게 목적이다.

**현황 확인:**

```
venv/Scripts/python verify_check.py
```

이 스크립트가 실제 DB·설정을 조회해 항목별로 `[DUE] / [READY] / [WAIT] / [BLOCKED] / [DONE]`을
찍어준다. 세션을 새로 시작했거나 오랜만에 손을 대면 이것부터 돌릴 것.

> **Claude에게:** 이 저장소에서 작업을 시작할 때, 그리고 사용자가 "뭐 확인할 거 없나 / 그거 어떻게
> 됐지"류로 물으면 위 스크립트를 먼저 돌리고 결과를 근거로 답할 것. 아래 기준을 기억에 의존해
> 판정하지 말 것 — 수치는 스크립트가 준다. 항목이 해결되면 이 파일에서 지우고,
> 새로 미루는 게 생기면 같은 형식으로 추가할 것.

마지막 갱신: 2026-09-04

---

## V1. AI 종목분석 신뢰도 — 기한 2026-09-25

**왜 미뤘나.** v3.138.0/1에서 종목 리포트에 정량 근거 팩·수급·실측 변동폭 앵커를 넣고 프롬프트의
하락 편향을 걷어냈다. 개선이 실제로 먹혔는지는 예측이 시간을 통과해봐야 알 수 있다.

**어떻게 판정하나.** `analysis_history`에 쌓인 단기 전망(`short_term_view_pct`)과 실제 주가를 대조.

**통과 기준.** 표본 20건+ AND 단기 전망 **방향** 적중률 55%+ AND 변동폭 절단 발생률이 초기 대비 하락.

**⚠️ 선행 조건 (지금 막혀 있음).** `analysis_history`에 사후 수익률 컬럼이 없다. 예측만 저장되고
결과를 채우는 경로가 없어서, **기한이 와도 판정이 불가능하다.** 기다린다고 해결되지 않는다.
→ `d1/d3/d7_return`, `outcome_checked_at` 컬럼 추가 + 일일 사후추적에 연결하는 수정이 먼저다.
V6과 뿌리가 같은 문제이므로 같이 처리하는 게 맞다.

---

## V2. 자체 ML 판단 반영 복귀 — 기한 2026-09-24

**왜 미뤘나.** 09-03에 실전 AUC가 d7 0.524 / d3 0.447로 나와 복귀 기준에 미달, 판단 반영 4곳을
차단(`ML_DECISIONS=0`)했다. 원인은 학습셋의 67%를 차지하던 `scenario` 소스가 예측력 0이었던 것.
이를 제외하고 재학습해 CV AUC가 0.606 → 0.705로 올랐다(v3.136.1). 이 개선이 실전에서도
재현되는지는 재학습 이후 새로 쌓인 표본으로만 확인할 수 있다.

**어떻게 판정하나.** `ml_training_samples`에서 `decided_at >= 2026-09-03`인 `pred_d7` + `label` 쌍만
골라 AUC를 다시 계산.

**통과 기준.** 신규 구간 실전 AUC **0.60+** → 판단 반영 4곳 복귀.

**⚠️ 함정.** CV 점수(0.705)로 판정하지 말 것. 그건 학습셋 위의 점수지 실전 성능이 아니다.
09-03에 이미 "CV는 좋은데 실전은 0.524"를 겪었다. 반드시 재학습 **이후** 구간만 볼 것.

---

## V3. 섀도우 리그 승자 이식 — 기한 없음(표본 기준), **이미 도달**

**왜 미뤘나.** 안 보이는 대조군 AI들을 병행 운용해 "어떤 매매 형태가 실제로 이기는가"를 실측하기로
했다(v3.118.0). 착수 기준선은 실현 30건이었다.

**현재.** 실현 172건으로 기준선을 훨씬 넘었다. 판정 재료가 이미 있다.

| 전략 | 건수 | 평균수익 | 승률 |
|---|---|---|---|
| SHADOW_A (눌림목) | 40 | +3.64% | 52.5% |
| SHADOW_C (이슈×구간) | 41 | +2.52% | 61.0% |
| SHADOW_E (랜덤 대조군) | 40 | -2.82% | 32.5% |
| SHADOW_F (모멘텀 추격) | 32 | -6.57% | 34.4% |

**통과 기준.** 랜덤 대조군(E) 대비 평균수익·승률 모두 우위 + 표본 30건+.
→ A와 C가 이미 충족. F(모멘텀 추격)는 대조군보다도 나쁘므로 메인에서 이 형태를 배제할 근거가 된다.

**다음 행동.** A·C의 진입 조건을 메인 판단에 어떻게 반영할지 논의. 사용자 결정 사항.

---

## V4. 수급 스냅샷 적재 — **해결 확인됨 (2026-09-04)**

6/23 중단됐던 원인(15분 창 의존)을 v3.107.1에서 고쳤고, 실제 적재를 확인했다.
`frgn_inst_snapshots` 3,111행, 최신 2026-09-03. 세력 이상급증 감지(v3.107.0)의 전제가 충족됐다.
스크립트가 계속 감시하므로 5일 이상 밀리면 다시 `[DUE]`로 뜬다.

---

## V5. 토스 Open API IP 허용목록 — 사용자 액션 대기

**증상.** 토큰 발급이 `403 access_denied: IP address not allowed`로 실패. 그 결과 종합검색의
호가창·최근체결이 비고, 장운영이 개장일에도 **"오늘 휴장"으로 거짓 표시**된다(조회 실패와 휴장을
화면이 구분하지 못함).

**해야 할 일.** 토스증권 개발자센터에서 현재 공인 IP를 허용목록에 등록. 유동 IP라 저절로
낫지 않는다.

**부수 과제.** 등록과 별개로, 조회 실패를 "휴장"으로 표시하는 UI를 "장운영 정보 없음"으로
고치는 게 맞다(사용자가 후순위로 미룸).

---

## V6. AI 추천 적중률 파이프라인 — **끊겨 있음, 기한 없음**

**증상.** `ai_recommendations` 테이블이 0행이다. 여기에 쓰는 유일한 경로인 `POST /api/ai-log`를
호출하는 곳이 프론트에 없다. 그래서 `api/main.py`의 일일 `track_ai_recommendation_outcomes()`가
매일 빈 테이블을 돌고 있고, d1/d3/d7 수익률 추적 기계 전체가 헛돌고 있다.

**왜 중요한가.** V1의 검증도, ML 학습 샘플의 `recommendation` 소스도 이 경로에 의존한다.
지금 구조에는 이력 테이블이 둘(`ai_recommendations` 구형 / `analysis_history` v3.137.0 신형)로
갈라져 있는데, 구형에는 추적 기계가 있고 신형에는 데이터가 들어온다 — 정확히 엇갈려 있다.

**제안.** 둘을 하나로 합칠 것. `analysis_history`에 수익률 컬럼을 붙이고 일일 추적을 그쪽으로
돌리는 편이 낫다(데이터가 실제로 들어오는 쪽이므로). V1의 선행 조건이기도 하다.

---

## 완료되어 내려간 항목

- (없음 — V4는 위에 기록만 남기고 다음 정리 때 삭제)
